# Core framework
Django>=5.0
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3

# Database
psycopg2-binary>=2.9

# Face recognition — InsightFace (ArcFace R100) + ONNX Runtime
# Replaces DeepFace/TensorFlow. 3x faster, better accuracy, no TF dependency.
insightface>=0.7.3
onnxruntime>=1.17.0          # CPU inference (use onnxruntime-gpu on GPU VPS)
opencv-python-headless>=4.8  # image I/O for InsightFace (headless = no GUI deps)

# Data processing
numpy>=1.24
Pillow>=10.0
pandas>=2.0

# Fuzzy name matching
rapidfuzz>=3.0

# Reporting
reportlab>=4.0
openpyxl>=3.1

# Runtime utilities
python-decouple>=3.8
gunicorn>=21.2

# Note: InsightFace downloads models automatically on first use to ~/.insightface/models/
# On a fresh VPS, pre-warm the models so the first request is not slow:
#
#   docker compose exec backend python -c "
#     from apps.core.face import get_face_app
#     app = get_face_app()
#     print('InsightFace buffalo_l ready:', app)
#   "
#
# Total download: ~500 MB (cached in insightface_models Docker volume).
# Use 'buffalo_m' (~150 MB) or 'buffalo_s' (~90 MB) for a smaller footprint.
